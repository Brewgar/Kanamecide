---
id: W-0006
type: work
title: "Root-scratch debt reduction: shrink root_grandfathered.txt with itemized deletions"
round: 4
owner: implementation-engineer
status: IN_PROGRESS
deliverable: "updated research/scripts/root_grandfathered.txt + itemized deletion/retention log in this work item"
exit_check: "research.py validate OK; every deleted file listed; e0010_report.py and E-0010 raw evidence retained (they reproduce a published result)"
evidence: ["research/scripts/root_grandfathered.txt shrunk 282 -> 271 lines (11 deletions, itemized below); validate OK, 0 problems after the shrink (research/context/bootstrap/validate_w0006.txt)", "hygiene classifier false-negative characterized with raw output: `e0010_k1n..k5n_games.jsonl` + `_result.txt` (10 files, ~1.13 MB, the per-term ladder R-0008 re-derived) are flagged 'unreferenced' because EV-0001 and E-0010 name them by range shorthand (k1n..k5n) — classification script + output in research/context/w0006_triage.py / w0006_triage_out.txt", "retention cross-check: E-0010's own hygiene note (kept: e0010_k{1..6}n_games.jsonl + result.txt, e0010_gates_bd.txt, _g0_perft.txt, e0010_runner_log.txt, e0010_ALL_done.txt, all e0010_* harness scripts) and EV-0001/EV-0002 (pin the raw JSONL + harness) — all confirmed still present on disk"]
verified_by: null
verification_verdict: null
example: false
created: 2026-09-14
closed: null
---

# W-0006 — Root-scratch debt reduction

## Objective
The repo root carries ~250 grandfathered scratch files (R-0003 F9 debt). Delete confirmed
junk, keep anything load-bearing, itemize every action. Nothing here is history-rewriting:
root scratch was never tracked evidence — but the list of what was removed must be public.

## Deliverable (exact path(s))
- `research/scripts/root_grandfathered.txt` (shrunk)
- Deletion/retention log appended below.

## Exit Check
```powershell
python research/scripts/research.py validate    # OK
python e0010_report.py                          # still reproduces every E-0010 number
```

## Evidence
- (to be filled)

## Work Log (append-only while OPEN)
- 2026-09-14 — opened by Round-4 meta-agent (R-0003 F9 / DEC-0009 hygiene policy).
- 2026-09-21 — adversarial-reviewer (W-0002 session, acting under the orchestrator's
  explicit instruction; owner seat for verification remains implementation-engineer):
  performed a BOUNDED shrink pass. `research.py hygiene` reports 232 unreferenced
  grandfathered candidates (sanctioned 6 / grandfathered 277 / unsanctioned-NEW 0).

  **Deleted (11) — unreferenced AND non-evidence AND regenerable/transient, verified by
  content and by a zero-hit grep across all `research/**/*.md`:**
  | file | class | why safe |
  |---|---|---|
  | `bitboard.obj`, `board.obj`, `main.obj`, `movegen.obj`, `perft.obj`, `zobrist.obj` | compile intermediates | regenerated by compiling; no evidentiary role (they are not the measured binaries) |
  | `_procs.txt`, `_procs2.txt`, `_plist.txt` | `Get-Process` listings | transient debug output (headers confirmed) |
  | `_alive.txt` (content: `RUNNING`), `_cpu.txt` (process CPU table) | heartbeat/debug markers | transient |

  `research/scripts/root_grandfathered.txt`: 282 → 271 lines (exactly those 11 names
  removed; script `research/context/w0006_edit_list.py`). `validate` after the shrink:
  **OK, 0 problems** (`research/context/bootstrap/validate_w0006.txt`).

  **Retained — with the reason (no e0010_* deletions, per the work item's exit check):**
  - ALL 46 `e0010*` root files kept, including the 30 that hygiene flags as candidates:
    every raw `e0010_k1n..k5n_games.jsonl` + `_result.txt` (per-term ladder, ~1.13 MB),
    `e0010_k1..k6[_err].txt`, `.bat` launchers, `e0010_pilot6*.txt` — E-0010's own
    hygiene note and EV-0001/EV-0002 pin this class.
  - `_g0_perft.txt`, `e0010_gates_bd.txt`, `e0010_runner_log.txt`, `e0010_ALL_done.txt`,
    all `e0010_*` harness scripts (E-0010 record + EV-0002).
  - `_repro_err_m1_e0..e11.txt` (12 files, ~9.5 MB): the only raw engine-trace forensics
    of the m1 stall/crash campaign (content check: `[GO]/[SR]` engine traces). Kept
    pending an owner decision — deletion would lose the incident's raw symptom record.
  - `kana_o3b.exe`, `kana_o3c.exe`: old engine binaries without recorded provenance
    (D-0002 rule: excluded from claims). Kept pending an owner decision — they are
    irreplaceable rebuilds of superseded revisions.

  **FINDING (recorded, not silently absorbed): the hygiene classifier's
  `referenced_in_records` flag under-counts.** Evidence: `e0010_k1n_games.jsonl` etc.
  are flagged unreferenced while EV-0001 (`path: e0010_k6n_games.jsonl` front-matter +
  body: "`e0010_k1n..k5n_games.jsonl` for the per-term ladder") and E-0010 ("k1n..k5n:
  200 games each") reference them by RANGE SHORTHAND — the literal-filename scan misses
  `k1n..k5n`. Consequence: 10 files (~1.13 MB) of published-result evidence appear
  deletable. Raw triage: `research/context/w0006_triage.py` + `w0006_triage_out.txt`
  (44 of the 232 candidates are `e0010*`/`_g0*`; the rest are mostly `_repro_*` dumps,
  old binaries, helper-script scratch). RECOMMENDATION: system owner fixes the reference
  scan to (a) expand `x..y` filename ranges and (b) treat EV-#### `path:`/body names as
  pins; only then should a full 232-file pass proceed. Do NOT trust the candidate flag
  as-is.
  - Item left **IN_PROGRESS** (not DONE): the owner seat (implementation-engineer) or a
    fresh verifier should confirm the 11 deletions + list shrink, decide the retained
    borderline classes (`_repro_err_m1_*`, old exes), and close it.
- 2026-09-21 — system owner (orchestrator session): **the recommendation is implemented.**
  `research.py` now derives the hygiene keep-set from record text, not just the literal
  substring scan (`_hygiene_keep_names`, selftests 47 OK):
  (a) backtick-quoted basenames in prose are extracted as exact pins;
  (b) `x..y` numeric ranges expand (`e0010_k1n..k5n_games.jsonl` → k1n…k5n, zero-pad aware);
  (c) brace ranges expand (`e0010_k{1..6}n_result.txt` → k1n…k6n);
  (d) glob citations (`e0010_*.jsonl`) are honored via fnmatch — with a guard rail:
      a glob needs a literal prefix of ≥3 chars before its first wildcard, because
      records cite `*_err.txt`, `*_log.txt` and `_*.txt` as DELETED classes (E-0010's
      hygiene note; R-0004/S-0001/S-0002 scratch notes) and honoring them would have
      protected the whole class and inverted the record's meaning.
  Before→after on the live corpus: candidates 218 → 134; the 84 newly protected files
  are exactly the shorthand/glob-cited classes (`e0010*` 46, `_repro_err_c12_*` 13,
  `_repro_err_m1_*` 12, `_g0*` 15, …); **zero regressions** (nothing previously
  protected is now flagged); no `e0010*` file remains a candidate. Proof artifacts:
  `research/context/_fix4_probe.py` + `_fix4_probe.txt` (before/after diff + glob
  breadth audit), `_fix5_ctx.py` + `_fix5_ctx.txt` (citation-context extraction that
  motivated the glob guard rail), `_hyg.txt` / `_fix4_hyg.txt` (raw before/after).
  The full 134-file pass can now proceed — with the caveat that glob protection is
  deliberately conservative: a reviewer should still eyeball each candidate before
  deleting. Item stays IN_PROGRESS for its owner/verifier.

## Verification
- verified_by: (a different agent than owner)
- verdict: (VERIFIED | CONTRADICTED | PARTIAL | UNVERIFIABLE)
- evidence: (review record id + command outputs)